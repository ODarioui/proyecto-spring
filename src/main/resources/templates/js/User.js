function registerUser(event) {
    // Evitar la redirección o recarga de la página

    alert("holaa")
    // Lógica de registro que quieres ejecutar
    const username = document.getElementById("username");
    const password = document.getElementById("password");

    // Hacer algo con los datos, como enviarlos mediante AJAX, por ejemplo
    console.log("Registrando usuario:", username);
    
    // Puedes agregar más lógica aquí según sea necesario, como mostrar un mensaje o realizar un envío asincrónico
}
