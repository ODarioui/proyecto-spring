spring.application.name=gestion-recursos-humanos


# Configuración de la base de datos
spring.datasource.url=jdbc:mysql://localhost:3306/RH_DB?useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=root

# Configuración del driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Configuración de JPA (Hibernate)
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update  # Puedes usar 'create', 'update', 'validate' o 'none'
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true  # Formatea las consultas SQL para mayor legibilidad

# Configuración de las transacciones
spring.transaction.rollback-on-commit-failure=true

#lskjsalkd